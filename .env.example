# Copy this file to `.env` and fill in your own MySQL settings.
#   cp .env.example .env
# Values set in your shell environment take priority over this file.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=quiz_user
DB_PASSWORD=quiz_pass
DB_NAME=quiz_db
